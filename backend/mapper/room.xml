<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "mybatis-3-mapper.dtd">

<mapper namespace="device">
    <!-- 사원 번호 넣으면 그 사원의 방 정보 조회 -->
    <select id="searchRoomByEmployeeNumber" parameterType="int" resultType="list">
        SELECT r.room_number, r.name, r.x, r.y
        From room r LEFT JOIN employee e
        ON r.room_number = e.room_number
        WHERE e.employee_number = ${employee_number}
    </select>
</mapper>